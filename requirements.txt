kivy==2.3.1
kivy_deps.sdl2==0.8.0
kivy_deps.glew==0.3.1
kivy_deps.angle==0.4.0

# For generating HTML (no heavy modules needed)
jinja2==3.1.4

# Utility packages
requests==2.32.3

# Unicode handling (optional but safe)
regex==2024.11.6
